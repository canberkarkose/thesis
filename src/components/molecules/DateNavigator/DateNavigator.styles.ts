import styled from '@emotion/styled';
import { Box } from '@mui/material';

export const DateNavigatorContainer = styled(Box)`
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 16px;
  min-height: 48px;
`;
